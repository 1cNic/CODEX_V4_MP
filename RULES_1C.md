# Стандарт разработки 1С

> **Версия:** 4.0-draft | **Дата:** 2026-04-19 | **Статус:** Рабочий стандарт
>
> Этот документ задаёт полный стандарт разработки на `1С:Предприятие 8.3` для задач любой сложности.
> Он работает совместно с `RULES_1C_QUICK.md`, который является коротким обязательным ядром (`CORE`).

---

## 0. Мандат документа

### 0.1 Как использовать этот документ

- `RULES_1C_QUICK.md` — короткий обязательный минимум для ежедневной работы.
- `RULES_1C.md` — полный стандарт с развилкой по профилям задач.
- `CODEX_MISTAKES_1C.md` — локальная память проекта по ошибкам и антипримерам.
- `PATTERNS_1C.md` — локальные удачные решения и повторно используемые эталоны.
- `RULES_1C_ARCHIVE_v3_2026-04-18.md` — архив полной предыдущей версии с большими примерами, которые еще не полностью перенесены в новую структуру.

### 0.1.1 Как не дублировать CORE

- `RULES_1C_QUICK.md` — это обязательное ядро, которое всегда считается прочитанным вместе с этим документом.
- `RULES_1C.md` не должен заново переписывать весь `CORE` в развернутом виде без причины.
- В полном стандарте допустимо следующее:
  - уточнять правило из `CORE`;
  - расширять его для профилей `EDU`, `PROD`, `ENT`;
  - добавлять примеры, развилки и исключения.
- В полном стандарте не следует:
  - повторять один и тот же короткий запрет в нескольких разделах;
  - копировать целые списки из `CORE`, если достаточно сослаться на общий принцип;
  - смешивать обязательный минимум и редкие расширенные сценарии в одном пункте.
- Если правило полностью покрывается `CORE`, в полном стандарте достаточно сослаться на него и описать только профильные нюансы.

### 0.2 Профили применимости

Каждое правило должно иметь:
- профиль применимости;
- уровень строгости.

Профили:
- `ALL` — правило действует всегда;
- `EDU` — учебные и тестовые задачи;
- `PROD` — прикладная промышленная разработка;
- `ENT` — сложные промышленные сценарии, интеграции, highload, наблюдаемость, CI/CD.

Уровни строгости:
- `MUST` — обязательно;
- `SHOULD` — рекомендуется;
- `MUST NOT` — запрещено.

Пример записи:
- `[ALL][MUST] Не использовать ВЫБРАТЬ *`.
- `[EDU][SHOULD] Для простого учебного отчета допустим запрос + форма + ТаблицаЗначений`.
- `[PROD][MUST] Для пользовательского аналитического отчета с вариантами использовать СКД`.
- `[ENT][SHOULD] Для длительных операций использовать фоновые задания и мониторинг выполнения`.

### 0.3 Как выбирать профиль задачи

| Тип задачи | Профиль |
|------------|---------|
| Учебная обработка, учебный документ, тестовое задание | `ALL + EDU` |
| Доработка типовой конфигурации, прикладная разработка в боевой базе | `ALL + PROD` |
| Интеграции, обмены, сложная СКД, фоновые задания, CI/CD, мониторинг | `ALL + PROD + ENT` |

### 0.4 Блокирующий принцип

- `[ALL][MUST]` Перед выдачей итогового кода или правок проходить все применимые чек-листы.
- `[ALL][MUST]` Если правило стандарта противоречит задаче, явно фиксировать противоречие и предлагать безопасную альтернативу.
- `[ALL][MUST NOT]` Выдавать итоговый код, если нарушены критичные правила профиля задачи.

---

## 1. Протокол работы AI по 1С

- `[ALL][MUST]` Перед любыми изменениями по 1С сначала сверяться с `CODEX_MISTAKES_1C.md`.
- `[ALL][MUST]` Потом проверять `PATTERNS_1C.md`, если в проекте уже есть подходящий успешный пример.
- `[ALL][MUST]` Потом искать локальный эталон в текущем проекте, `samples_1c` или `tools/reference_indexes`.
- `[ALL][MUST]` После выбора подхода делать одну реализацию без архитектурных прыжков.
- `[ALL][MUST]` Если точного эталона нет, честно остановиться и сказать, что нужен образец, а не сочинять.

### 1.1 Что считать локальным эталоном

- рабочий файл из текущего проекта;
- подтвержденный шаблон из `samples_1c`;
- индекс или reference-файл проекта;
- только после этого — внешние источники.

### 1.2 Что запрещено угадывать

- `[ALL][MUST NOT]` XML-структуру метаданных;
- `[ALL][MUST NOT]` управляемые формы и события форм;
- `[ALL][MUST NOT]` имена форм и параметры `ОткрытьФорму(...)`;
- `[ALL][MUST NOT]` `cfg:CatalogRef...`, `cfg:CatalogObject...`, `cfg:Document...`, `cfg:EnumRef...`;
- `[ALL][MUST NOT]` `WindowOpeningMode`, `Attributes`, `Commands`, `ChildItems`, `StandardAttributes`;
- `[ALL][MUST NOT]` подменять стандартное поведение формы пользовательским кодом без подтвержденного локального примера.

### 1.3 Формат результата

- `[ALL][MUST]` Код должен быть готов к вставке.
- `[ALL][MUST NOT]` Оставлять заглушки и комментарии вида «тут ваш код».
- `[ALL][SHOULD]` Для решений, которые будут повторяться, пополнять `PATTERNS_1C.md`.
- `[ALL][SHOULD]` Для новых реальных ошибок пополнять `CODEX_MISTAKES_1C.md`.

---

# PART A — УНИВЕРСАЛЬНОЕ ЯДРО (`ALL`)

## 2. Запросы

### 2.1 Выборка полей и параметры

- `[ALL][MUST]` Не использовать `ВЫБРАТЬ *`.
- `[ALL][MUST]` Все поля перечислять явно и задавать алиасы через `КАК`.
- `[ALL][MUST]` Все значения передавать только через параметры `&ИмяПараметра`.
- `[ALL][MUST]` Устанавливать параметры через `Запрос.УстановитьПараметр()`.
- `[ALL][MUST NOT]` Конкатенировать значения в текст запроса.

### 2.2 Отборы, соединения, сортировка

- `[ALL][MUST]` Отборы размещать как можно ближе к источнику данных.
- `[ALL][MUST NOT]` Применять функции к полям в `ГДЕ`, если можно отобрать по диапазону или индексируемому значению.
- `[ALL][SHOULD]` Минимизировать количество соединяемых таблиц.
- `[ALL][SHOULD]` Использовать `ВНУТРЕННЕЕ СОЕДИНЕНИЕ`, когда правая часть гарантированно существует.
- `[ALL][SHOULD]` Использовать `ЛЕВОЕ СОЕДИНЕНИЕ`, когда нужно сохранить полный набор записей левого источника.
- `[ALL][MUST]` После `ЛЕВОЕ СОЕДИНЕНИЕ` не фильтровать поля правой таблицы в `ГДЕ`, если нужно сохранить все строки левого источника.
- `[ALL][MUST]` Учитывать, что порядок и вложенность соединений при 3+ источниках влияют на результат запроса.
- `[ALL][SHOULD]` Не сортировать в запросе без реальной необходимости.

### 2.3 Пустота, NULL, агрегаты

- `[ALL][MUST]` Перед обработкой результата проверять, что выборка не пустая.
- `[ALL][MUST]` Различать `NULL`, пустую строку, пустую ссылку и `Неопределено`.
- `[ALL][MUST]` `NULL` проверять через `ЕСТЬ NULL`.
- `[ALL][MUST]` Для фильтрации по агрегатам использовать `ИМЕЮЩИЕ`, а не `ГДЕ`.
- `[ALL][MUST]` В `СГРУППИРОВАТЬ ПО` указывать реальные имена полей, а не псевдонимы.
- `[ALL][MUST]` `ПРЕДСТАВЛЕНИЕ()` использовать только для вывода пользователю.

### 2.4 Иерархия, временные таблицы, оформление

- `[ALL][SHOULD]` Для иерархии использовать `В ИЕРАРХИИ`, если нужен весь подчиненный узел.
- `[ALL][SHOULD]` Для сложных и повторно используемых выборок предпочитать временные таблицы `ПОМЕСТИТЬ`.
- `[ALL][SHOULD]` Временные таблицы индексировать по полям, которые участвуют в отборах и соединениях.
- `[ALL][MUST]` Текст запроса оформлять аккуратно и единообразно.
- `[ALL][MUST]` Если текст из запроса попадает в интерфейс, использовать `НСтр()`.

## 3. Код BSL

### 3.1 Базовые правила

- `[ALL][MUST]` В коде BSL не оставлять заглушки и комментарии вида «тут ваш код».
- `[ALL][SHOULD]` Повторно используемую логику выносить в общие модули.
- `[ALL][MUST NOT]` Создавать большие универсальные `god`-модули.
- `[ALL][MUST]` Экспортировать только действительно нужные процедуры и функции.
- `[ALL][SHOULD]` Начинать работу со ссылки; к `Объекту` переходить только когда действительно нужно менять данные.
- `[ALL][MUST]` В модуле объекта использовать `ЭтотОбъект`, в модуле формы `Объект`.

### 3.1.1 Типы данных и приведение

- `[ALL][MUST]` Явно приводить типы там, где сравнение или вычисление может дать неоднозначный результат.
- `[ALL][MUST]` Различать `Неопределено`, пустую ссылку, пустую строку и `NULL`.
- `[ALL][SHOULD]` Для ручных таблиц и структур данных задавать ожидаемые типы явно.

### 3.2 Структура и читаемость

- `[ALL][SHOULD]` Использовать ранний `Возврат`, если это упрощает структуру кода.
- `[ALL][SHOULD]` Не прятать тяжелые вычисления в `?()`.
- `[ALL][SHOULD]` Явно приводить типы там, где поведение неоднозначно.
- `[ALL][SHOULD]` Документировать экспортные процедуры и функции.
- `[ALL][MUST]` Если поведение кода непонятно, сначала воспроизводить его, а не додумывать.

### 3.3 Исключения и коллекции

- `[ALL][MUST]` Исключения обрабатывать осознанно и отделять ожидаемые прикладные ошибки от технических.
- `[ALL][SHOULD]` Для табличных наборов использовать `ТаблицаЗначений`, если это действительно упрощает решение.
- `[ALL][SHOULD]` При выборе коллекции учитывать назначение, а не тянуть `ТаблицаЗначений` по умолчанию везде.
- `[ALL][MUST]` Выбирать коллекцию по назначению:
  - `Массив` — упорядоченный список;
  - `Структура` — параметры по строковым ключам;
  - `Соответствие` — произвольные ключи;
  - `ТаблицаЗначений` — табличный буфер данных;
  - `ДеревоЗначений` — иерархия.
- `[ALL][SHOULD]` Использовать фиксированные коллекции при передаче данных, которые нельзя модифицировать.

## 4. Формы, клиент и сервер

### 4.1 Разделение ответственности

- `[ALL][MUST]` UI и обработчики формы держать на клиенте.
- `[ALL][MUST]` Запросы, проведение, расчеты и работу с данными выносить на сервер.
- `[ALL][MUST]` Явно указывать директивы `&НаКлиенте`, `&НаСервере`, `&НаСервереБезКонтекста`.
- `[ALL][SHOULD]` Если контекст формы не нужен, предпочитать `&НаСервереБезКонтекста`.
- `[ALL][SHOULD]` Минимизировать передачу данных между клиентом и сервером.

### 4.2 Поведение формы

- `[ALL][MUST NOT]` Размещать бизнес-логику в `ПриОткрытии`, если по сценарию нужна кнопка.
- `[ALL][SHOULD]` Если нужна гарантированно видимая кнопка, добавлять ее явно по локальному эталону.
- `[ALL][SHOULD]` Предпочитать немодальный подход в управляемом приложении.
- `[ALL][SHOULD]` Для динамических списков учитывать стандартное поведение платформы и не подменять его без веской причины.

## 5. Транзакции и производительность

### 5.1 Транзакции

- `[ALL][MUST]` Транзакцию открывать как можно позже и закрывать как можно раньше.
- `[ALL][MUST NOT]` Вызывать интерактивные методы внутри транзакции.
- `[ALL][MUST]` Внутри транзакций использовать `Попытка...Исключение`.
- `[ALL][MUST]` Если используется ручное управление транзакцией, предусматривать корректное завершение в обеих ветках.

### 5.2 Производительность

- `[ALL][SHOULD]` Для больших выборок и сервисов использовать пагинацию.
- `[ALL][SHOULD]` При кэшировании продумывать инвалидирование кэша.
- `[ALL][SHOULD]` Для производительных сценариев избегать лишних чтений объекта, если достаточно ссылки или запроса.

### 5.3 Асинхронность и фоновые задания

- `[ALL][SHOULD]` Для длительных пользовательских операций использовать асинхронный сценарий или фоновые задания.
- `[ALL][MUST]` Не блокировать интерфейс тяжелой синхронной обработкой, если задача допускает фоновое выполнение.
- `[ALL][SHOULD]` Для фоновых и регламентных заданий предусматривать контроль статуса, ошибки и повторный запуск.

## 6. Объекты метаданных и XML

### 6.1 Общие правила

- `[ALL][MUST]` Все правки `Configuration.xml`, объектов метаданных, форм и `Ext/Form.xml` делать только по локальному эталону.
- `[ALL][MUST]` Перед правкой проверять имя объекта, путь файла, `ChildObjects`, формы по умолчанию и ссылки в `Configuration.xml`.
- `[ALL][MUST]` При переименовании объекта проверять XML, формы, команды, ссылки в коде и пути файлов как одну целостную операцию.
- `[ALL][MUST NOT]` Считать изменившийся текст ошибки доказательством правильного решения.
- `[ALL][SHOULD]` Проверять, не является ли новая ошибка следствием предыдущей импровизации.

### 6.2 Обязательные проверки

- `[ALL][MUST]` Новый объект должен быть добавлен в `Configuration.xml`, если это новый объект.
- `[ALL][MUST]` Имя объекта должно совпадать с путем файла.
- `[ALL][MUST]` Не должно оставаться хвостов от старого имени после переименования.
- `[ALL][MUST]` Не должно быть обращения к несуществующим функциям платформы.

## 7. Безопасность и интеграции

### 7.1 Базовая безопасность

- `[ALL][MUST NOT]` Хранить пароли, сертификаты и секреты в открытом виде.
- `[ALL][MUST]` Использовать `ЗащищённоеХранилище` и безопасные механизмы БСП.
- `[ALL][MUST]` Для внешних интеграций использовать HTTPS.

### 7.2 HTTP и JSON

- `[ALL][MUST]` В HTTP-сервисах возвращать корректные коды ответа.
- `[ALL][MUST]` В HTTP-ответах явно задавать `Content-Type` и кодировку.
- `[ALL][MUST]` Тело HTTP-ответа разбирать только после проверки `КодСостояния`.
- `[ALL][SHOULD]` Формат даты в JSON выбирать явно.
- `[ALL][SHOULD]` Для больших JSON использовать потоковые `ЧтениеJSON` и `ЗаписьJSON`.

### 7.3 Базовая прикладная безопасность

- `[ALL][MUST]` Все динамические значения в запросах передавать через параметры.
- `[ALL][MUST NOT]` Строить условия запроса конкатенацией строк.
- `[ALL][SHOULD]` Для криптографии использовать штатные механизмы платформы.
- `[ALL][MUST]` Сертификаты и чувствительные данные хранить в защищенном хранилище, а не в коде.

---

# PART B — УЧЕБНЫЕ ЗАДАЧИ (`EDU`)

## 8. Принципы учебной разработки

- `[EDU][MUST]` Решение должно в первую очередь соответствовать ТЗ, а не абстрактной “идеальной архитектуре”.
- `[EDU][SHOULD]` Для учебной конфигурации выбирать простое и прозрачное решение.
- `[EDU][MUST NOT]` Тащить тяжелую промышленную архитектуру без прямой необходимости.
- `[EDU][SHOULD]` Предпочитать читаемость, повторяемость и простую отладку.
- `[EDU][SHOULD]` Для учебного проекта выбирать минимально достаточный состав объектов.

## 9. Документы, регистры, проведение

- `[EDU][SHOULD]` Для учебного документа держать состав реквизитов и табличных частей минимально достаточным.
- `[EDU][SHOULD]` Для контроля остатков предпочитать простой и прозрачный алгоритм.
- `[EDU][MUST]` Понятные ошибки проведения показывать пользователю явным текстом.
- `[EDU][SHOULD]` Если внутренние имена можно делать русскими без нарушения правил платформы, использовать русские имена.
- `[EDU][SHOULD]` При учебном проведении приоритет отдается предсказуемости и проверяемости, а не “универсальности”.
- `[EDU][SHOULD]` Для учебных регистров измерения выбирать только по реальной потребности группировки и срезов.

## 10. Учебные отчеты

- `[EDU][SHOULD]` Для простого учебного отчета допустим запрос + форма + `ТаблицаЗначений`, если это соответствует ТЗ.
- `[EDU][MUST NOT]` Собирать программную СКД с нуля без подтвержденного локального шаблона.
- `[EDU][SHOULD]` Для отчета по регистру остатков использовать самый прозрачный путь вывода данных.
- `[EDU][SHOULD]` Не тянуть тяжелую пользовательскую аналитику и варианты отчета, если задача этого не требует.

## 11. Минимально достаточная проверка

- `[EDU][MUST]` Проверять основной пользовательский сценарий.
- `[EDU][SHOULD]` Проверять хотя бы один негативный сценарий.
- `[EDU][SHOULD]` Кратко фиксировать, что было проверено вручную пользователем.
- `[EDU][SHOULD]` Явно фиксировать, что запуск и загрузка в 1С выполнялись пользователем вручную, если это так.

---

# PART C — ПРИКЛАДНАЯ ПРОМЫШЛЕННАЯ РАЗРАБОТКА (`PROD`)

## 12. Общие принципы

- `[PROD][MUST]` Проектировать решение с учетом обновляемости и сопровождения.
- `[PROD][MUST]` Использовать БСП и типовые механизмы, если они уже являются частью прикладного решения.
- `[PROD][MUST NOT]` Создавать дублирующие механизмы там, где уже есть штатный механизм платформы или БСП.
- `[PROD][MUST]` Разделять прикладную логику, инфраструктурную логику и интеграционный слой.

## 13. БСП, типовые конфигурации, расширения

- `[PROD][MUST]` Соблюдать суффиксы модулей БСП и их назначение.
- `[PROD][MUST]` Для доработок типовых конфигураций предпочитать расширения, если это допустимо по задаче.
- `[PROD][MUST]` Не ломать обновляемость ради локального удобства.
- `[PROD][SHOULD]` Использовать подписки и точки расширения вместо прямого вмешательства в типовой код.
- `[PROD][MUST]` Для стандартных объектов прикладной конфигурации придерживаться типовой архитектуры БСП.

### 13.1 Печатные формы

- `[PROD][MUST]` Размещать код печатной формы в отдельном общем модуле с суффиксом `Печать`.
- `[PROD][MUST]` Использовать макеты типа `ТабличныйДокумент`.
- `[PROD][SHOULD]` Для пакетной печати объединять печатные формы через `ТабличныйДокумент.Вывести()`.
- `[PROD][SHOULD]` Для печатной формы выставлять `ТолькоПросмотр = Истина`, если пользователь не должен менять результат.

### 13.2 Файловые операции

- `[PROD][MUST]` Для хранения файлов в ИБ использовать БСП `ПрисоединённыеФайлы`, если она есть в проекте.
- `[PROD][MUST]` Перед сохранением валидировать тип и размер файла.
- `[PROD][SHOULD]` Для выгрузки больших файлов использовать `ВременноеХранилище`.

### 13.3 Email-рассылка

- `[PROD][MUST]` Для отправки писем использовать БСП `Почта`, если она является штатным механизмом проекта.
- `[PROD][SHOULD]` Для больших объёмов отправлять письма асинхронно или через фоновые задания.
- `[PROD][MUST]` Ошибки отправки письма фиксировать в журнале регистрации.
- `[PROD][SHOULD]` Для HTML-писем использовать шаблоны и локализацию.

### 13.4 Workflow и согласования

- `[PROD][MUST]` Для статусов согласования использовать перечисление, а не произвольные строки.
- `[PROD][SHOULD]` Журнал согласований хранить в регистре сведений.
- `[PROD][SHOULD]` Для сложных маршрутов использовать штатные механизмы БСП или бизнес-процессы, если они приняты в проекте.
- `[PROD][SHOULD]` Для pending-задач использовать уведомления и напоминания.

### 13.5 Мультикомпания и мультивалюта

- `[PROD][MUST]` Для решений с несколькими организациями учитывать `Организация` в регистрах и правах доступа.
- `[PROD][SHOULD]` Если данные должны разделяться по организациям на уровне доступа, использовать RLS или эквивалентный штатный механизм.
- `[PROD][MUST]` Для валютных операций хранить сумму в валюте документа и сумму в базовой валюте по курсу на дату.
- `[PROD][SHOULD]` Курсовые разницы рассчитывать на отчетную дату или дату погашения по принятой учетной модели.

### 13.6 Миграции и обработчики обновления

- `[PROD][MUST]` Обработчики обновления размещать в `ОбновлениеИнформационнойБазы`, если проект использует этот типовой механизм.
- `[PROD][MUST]` При переименовании реквизитов сохранять данные через явный перенос, а не надеяться на автоматическую магию.
- `[PROD][SHOULD]` Миграции тестировать на копии базы перед применением.
- `[PROD][SHOULD]` Большие обмены и переносы остатков выполнять пакетно.

### 13.7 Расширения конфигурации

- `[PROD][MUST]` В расширениях не удалять объекты основной конфигурации.
- `[PROD][MUST NOT]` Изменять типы существующих реквизитов основной конфигурации.
- `[PROD][SHOULD]` Для логики использовать подписки на события и точки расширения.
- `[PROD][SHOULD]` Объекты расширения помечать понятным префиксом проекта или подсистемы.

### 13.8 Роли и RLS

- `[PROD][MUST]` Проверять права доступа до выполнения операции, если это не гарантируется штатным механизмом.
- `[PROD][SHOULD]` Для разделения данных по пользователям, организациям и зонам ответственности использовать RLS или эквивалентный штатный механизм.
- `[PROD][MUST NOT]` Реализовывать безопасность только на уровне интерфейса без серверной проверки.

## 14. Отчеты и пользовательская аналитика

- `[PROD][MUST]` Для пользовательских аналитических отчетов с настройками и вариантами использовать СКД.
- `[PROD][MUST]` Если меняются только поля, оформление, сортировка, отборы или представление, делать новый вариант отчета, а не новый отчет.
- `[PROD][SHOULD]` Проектировать отчет так, чтобы пользовательские настройки и варианты были устойчивыми к доработкам.
- `[PROD][SHOULD]` Для параметров, которые всегда участвуют в получении данных, использовать признак `Всегда`.

## 15. Ошибки, логирование, сопровождение

- `[PROD][MUST]` Разделять сообщение пользователю и техническую ошибку для разработчика или администратора.
- `[PROD][MUST]` Ключевые ошибки фиксировать в журнале регистрации.
- `[PROD][SHOULD]` Важные прикладные операции делать наблюдаемыми и диагностируемыми.
- `[PROD][SHOULD]` Для сопровождаемого кода фиксировать точки входа, параметры и ожидаемые побочные эффекты.

## 16. Тестирование и релизная дисциплина

- `[PROD][MUST]` Перед релизом проверять ключевые сценарии.
- `[PROD][SHOULD]` Автоматизировать синтаксический контроль и тесты.
- `[PROD][SHOULD]` Проектировать миграции данных безопасными к повторному запуску.
- `[PROD][SHOULD]` Перед релизом приоритетно проверять расчеты, движения документов, права доступа и пограничные случаи.

---

# PART D — СЛОЖНЫЕ ПРОМЫШЛЕННЫЕ СЦЕНАРИИ (`ENT`)

## 17. Интеграции

- `[ENT][MUST]` Для сложных интеграций явно проектировать формат обмена, обработку ошибок, идемпотентность и повторный запуск.
- `[ENT][SHOULD]` При серии вызовов одного сервиса переиспользовать `HTTPСоединение`.
- `[ENT][SHOULD]` Для больших JSON использовать потоковые `ЧтениеJSON` и `ЗаписьJSON`.
- `[ENT][SHOULD]` Для OData, COM и обменов с внешними системами документировать ограничения и характер отказов.

### 17.1 HTTP-сервисы

- `[ENT][MUST]` Возвращать корректные HTTP-коды: `200`, `201`, `400`, `404`, `500`.
- `[ENT][SHOULD]` Использовать JSON для тела запроса и ответа.
- `[ENT][SHOULD]` Явно устанавливать `Content-Type` и кодировку ответа, обычно `application/json` и `UTF-8`.
- `[ENT][SHOULD]` Идентификаторы ресурса передавать в URL, а фильтры и режимы выборки — в параметрах запроса.

```bsl
Функция OrdersGET(Запрос)
    Заказ = Документы.ЗаказПокупателя.НайтиПоКоду(Запрос.ПараметрыURL.orderId);
    Если Заказ.Пустая() Тогда
        Возврат Новый HTTPСервисОтвет(404);
    КонецЕсли;

    JSON = ЗаписатьJSON(Новый Структура("id", Заказ.Код));
    Ответ = Новый HTTPСервисОтвет(200);
    Ответ.УстановитьТелоИзСтроки(JSON, КодировкаТекста.UTF8, "application/json");
    Возврат Ответ;
КонецФункции
```

### 17.2 HTTP-клиент, OData, COM

- `[ENT][SHOULD]` Разбирать тело ответа только после проверки `КодСостояния`.
- `[ENT][SHOULD]` Для OData учитывать, что ограничения платформы и RLS применяются автоматически.
- `[ENT][MUST NOT]` Хранить пароли для COM-соединения в открытом виде.
- `[ENT][SHOULD]` Если нужна кроссплатформенность, предпочитать HTTP-сервисы вместо COM.

## 18. Асинхронность, фоновые задания, длительные операции

- `[ENT][SHOULD]` Все операции дольше 5 секунд выносить в фоновые задания или асинхронный сценарий.
- `[ENT][MUST]` Проектировать безопасную повторную постановку и завершение фоновых заданий.
- `[ENT][SHOULD]` Обеспечивать контроль статуса, отмены и диагностики выполнения.
- `[ENT][SHOULD]` Для регламентных заданий учитывать защиту от параллельного повторного запуска.
- `[ENT][SHOULD]` Для длинных циклов обработки поддерживать индикацию прогресса и флаг отмены.

## 19. Кэширование, блокировки, нагрузка

- `[ENT][SHOULD]` Для кэша проектировать TTL и инвалидирование.
- `[ENT][MUST]` При конкурентной работе учитывать конфликты блокировок и повторные попытки.
- `[ENT][SHOULD]` Для высоконагруженных выборок использовать пагинацию, индексацию и предварительные наборы данных.
- `[ENT][SHOULD]` Для конкурентных сценариев учитывать оптимистические блокировки и версионность.

## 20. CI/CD, наблюдаемость и инфраструктура качества

- `[ENT][SHOULD]` Автоматизировать синтаксический контроль, тесты и сборку.
- `[ENT][SHOULD]` Настраивать технологический журнал и ключевые точки логирования.
- `[ENT][SHOULD]` Использовать метрики и мониторинг для длительных и критичных процессов.
- `[ENT][SHOULD]` Примеры пайплайнов, `logcfg.xml` и шаблонов логирования хранить в приложениях или архивной версии документа.

### 20.1 Минимальный CI-пайплайн

- `[ENT][SHOULD]` Минимальный пайплайн включает:
  - синтаксический контроль;
  - статический анализ, если он принят в проекте;
  - тесты;
  - сборку артефакта для релизной ветки.

```yaml
# .github/workflows/ci.yml
name: 1C CI/CD

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  syntax-check:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Syntax check
        run: |
          $v8 = "C:\Program Files\1cv8\8.3.25.1200\bin\1cv8.exe"
          & $v8 DESIGNER /F "${{ github.workspace }}\src" /CheckAllConfigFiles /Out "syntax.log"
```

### 20.2 Наблюдаемость

- `[ENT][SHOULD]` Для сложных сценариев настраивать технологический журнал.
- `[ENT][SHOULD]` Ключевые операции логировать единообразно.
- `[ENT][SHOULD]` Для наблюдаемости разделять пользовательское сообщение и техническую диагностику.

### 20.3 Инструменты автоматической проверки

- Этот подраздел собран в инфраструктурном блоке, но его инструменты применимы и для `PROD`, и для `ENT`.
- `[PROD][SHOULD]` Для прикладных сценариев использовать Vanessa Automation.
- `[PROD][SHOULD]` Для модульных проверок использовать `xUnitFor1C`.
- `[PROD][SHOULD]` Использовать `bsl-language-server` для автоматической проверки качества кода.
- `[PROD][SHOULD]` Перед релизом в первую очередь покрывать тестами расчёты, движения документов, права доступа и пограничные случаи.

---

# PART E — ВЫБОР АРХИТЕКТУРЫ ОТЧЕТА

## 21. Когда делать простой отчет на форме

- `[EDU][SHOULD]` Если задача учебная, отчет простой, пользователю не нужны варианты и расширенные настройки.
- `[ALL][SHOULD]` Если нужен быстрый и прозрачный вывод данных без сложной аналитики.
- `[ALL][SHOULD]` Если отчет проще поддерживать как запрос + форма, чем как самодельную программную СКД без макета.

## 22. Когда делать СКД

- `[PROD][MUST]` Если нужны варианты отчета, пользовательские настройки, группировки, ресурсы, расшифровки, отборы и типовое поведение.
- `[PROD][SHOULD]` Если отчет будет развиваться и обслуживаться в прикладной системе.
- `[PROD][MUST]` Для таблиц и диаграмм в СКД явно определять ресурсы.

## 23. Когда нужен ТабличныйДокумент

- `[ALL][SHOULD]` Если нужен печатный или близкий к печатной форме результат.
- `[PROD][SHOULD]` Если отчет должен соответствовать утвержденному макету, выбирать `ТабличныйДокумент`.
- `[PROD][SHOULD]` Для печатных форм и регламентированных представлений использовать `ТабличныйДокумент` как основной формат.

---

# PART F — XML И УПРАВЛЯЕМЫЕ ФОРМЫ

## 24. Жесткие правила

- `[ALL][MUST]` Перед изменением XML и форм сначала находить точный локальный эталон.
- `[ALL][MUST]` Проверять `Configuration.xml`, путь файла, имя объекта, `ChildObjects`, формы по умолчанию и связанные XML-файлы.
- `[ALL][MUST NOT]` Угадывать имя формы открытия в `ОткрытьФорму(...)`.
- `[ALL][MUST NOT]` Угадывать имя события формы в XML.
- `[ALL][MUST NOT]` Угадывать `WindowOpeningMode`, `Attributes`, `Commands`, `ChildItems`, `StandardAttributes`.
- `[ALL][MUST NOT]` Угадывать QName и `cfg:...` ссылки.
- `[ALL][MUST]` Для форм, каталогов, документов, регистров и перечислений использовать только подтвержденный образец структуры.

### 24.1 Критические правила XML объектов

- `[ALL][MUST]` `InternalInfo` обязателен для `Catalog`, `Document`, `InformationRegister`, `AccumulationRegister`.
- `[ALL][MUST NOT]` `InternalInfo` не добавляется в `Form` и `Subsystem`.
- `[ALL][MUST]` Для объектов метаданных использовать корректные `xr:ValueId`.
- `[ALL][MUST]` Для предопределенных элементов использовать отдельный `Ext/Predefined.xml`, если этого требует тип объекта.

### 24.2 Формы и табличные части

- `[ALL][MUST]` Для формы использовать корректный namespace объекта формы.
- `[ALL][MUST NOT]` Добавлять в форму неподтвержденные свойства без локального эталона.
- `[ALL][MUST]` Для табличных частей проверять служебную структуру и generated types по эталону.
- `[ALL][MUST NOT]` Придумывать состав служебных узлов табличной части по памяти.

### 24.3 ChildObjects и UUID

- `[ALL][MUST]` `ChildObjects` должен соответствовать типу объекта и его фактическому составу.
- `[ALL][MUST]` Если формы объекта реально существуют, они должны быть перечислены в `ChildObjects`.
- `[ALL][MUST]` UUID должны быть уникальными и не являться заглушками.
- `[ALL][MUST NOT]` Оставлять случайные или временные идентификаторы в итоговом проекте.

## 25. Проверка перед передачей проекта на загрузку

- `[ALL][MUST]` Если объект новый, он добавлен в `Configuration.xml`.
- `[ALL][MUST]` Имя объекта совпадает с путем файла.
- `[ALL][MUST]` Не осталось хвостов от старого имени после переименования.
- `[ALL][MUST]` Формы, которые реально существуют у объекта, перечислены в `ChildObjects`, а формы по умолчанию ссылаются на реально существующие формы.
- `[ALL][MUST]` Нет автозапуска логики из формы, если по сценарию нужна кнопка.
- `[ALL][MUST]` Нет обращения к несуществующим функциям платформы.
- `[ALL][SHOULD]` Перед ручной загрузкой проекта проверять целостность путей и связанных файлов объекта.

---

# PART G — SELF-CHECK

## 26. Общий принцип

- `[ALL][MUST]` Применять блокирующий принцип из `0.4` и перед выдачей итогового кода проходить все применимые чек-листы.
- `[ALL][MUST]` Не выдавать итоговый код, если хотя бы один критичный пункт чек-листа не выполнен.
- `[ALL][SHOULD]` Чек-листы в этом разделе должны проверять критичный минимум, а не дублировать целиком все разделы стандарта.

## 27. CORE checklist (`ALL`)

Проверить:
- нет `ВЫБРАТЬ *`;
- все параметры передаются через `УстановитьПараметр`;
- клиент и сервер разделены;
- XML и формы правятся только по эталону;
- код готов к вставке;
- сообщение пользователю отделено от технической причины;
- не повторена известная ошибка из `CODEX_MISTAKES_1C.md`.

## 28. EDU checklist

Проверить:
- решение соответствует ТЗ;
- архитектура не переусложнена;
- сценарий прост и прозрачен;
- пользователь получает понятный результат;
- сложность решения соразмерна учебной задаче.

## 29. PROD checklist

Проверить:
- учтены БСП и типовые механизмы;
- решение сопровождаемо и обновляемо;
- учтены логирование и ошибки;
- отчет или интеграция соответствуют прикладной задаче;
- не создан дубль штатного механизма.

## 30. ENT checklist

Проверить:
- учтены фоновые задания и длительные операции;
- интеграция устойчива к повторному запуску;
- есть наблюдаемость, мониторинг или диагностические точки;
- учтены производительность и конкурентная работа;
- объем инфраструктуры оправдан реальной сложностью задачи.

---

## Приложения

### A. Большие примеры CI/CD

Сюда переносить:
- GitHub Actions;
- Jenkinsfile;
- Vanessa Automation;
- сценарии сборки и публикации.

До полного переноса смотри `RULES_1C_ARCHIVE_v3_2026-04-18.md`.

#### A.1 Минимальный принцип

- синтаксический контроль обязателен первым этапом;
- тесты запускаются после синтаксического контроля;
- сборка артефакта выполняется только после успешных тестов;
- публикация отделяется от проверки.

#### A.2 Минимальный сценарий тестирования

- `Vanessa Automation` — для BDD и прикладных пользовательских сценариев;
- `xUnitFor1C` — для изолированных модульных проверок;
- `bsl-language-server` — для статического анализа до запуска тестов.

### B. Большие примеры наблюдаемости

Сюда переносить:
- `logcfg.xml`;
- OpenMetrics;
- Grafana/Prometheus;
- шаблоны централизованного логирования.

До полного переноса смотри `RULES_1C_ARCHIVE_v3_2026-04-18.md`.

#### B.1 Минимальный `logcfg.xml`

```xml
<config xmlns="http://v8.1c.ru/v8/techlog">
  <log history="24" location="C:\1C\Logs">
    <event>
      <eq property="name" value="EXCP"/>
      <eq property="level" value="error"/>
    </event>
    <property value="excptext;desc"/>
  </log>
</config>
```

#### B.2 Единый паттерн логирования

```bsl
&НаСервере
Процедура Логирование.Записать(Уровень, Источник, Сообщение, Данные = Неопределено) Экспорт
    ЗаписьЖурналаРегистрации(
        Источник,
        Уровень,
        ,
        ,
        Сообщение + ?(Данные = Неопределено, "", " | " + ЗначениеВСтрокуВнутр(Данные))
    );
КонецПроцедуры
```

### C. Большие reference snippets

Сюда переносить:
- длинные примеры XML;
- большие шаблоны отчетов;
- интеграционные заготовки;
- редкие и специализированные примеры.

До полного переноса смотри `RULES_1C_ARCHIVE_v3_2026-04-18.md`.

#### C.1 HTTP-клиент

```bsl
Соединение = Новый HTTPСоединение("api.example.com");
Ответ = Соединение.Получить(Новый HTTPЗапрос("/v1/products"));
Если Ответ.КодСостояния = 200 Тогда
    Данные = ПрочитатьJSON(Ответ.ПолучитьТелоКакСтроку());
КонецЕсли;
```

#### C.2 COM-подключение

```bsl
V8 = Новый COMОбъект("V83.COMConnector");
Пароль = ХранилищеСистемныхНастроек.Загрузить("ExternalBasePassword");
База = V8.Connect("File=C:\Base;Usr=admin;Pwd=" + Пароль);
База = Неопределено;
```

#### C.3 Vanessa Automation

```gherkin
# language: ru
Функционал: Создание документа Реализация
    Сценарий: Создание и проведение
        Дано я открываю список документов "РеализацияТоваровУслуг"
        Когда я создаю новый документ
        И я заполняю поле "Контрагент" значением "ООО Ромашка"
        И я записываю документ
        Тогда документ проведён
```

#### C.4 xUnitFor1C

```bsl
#НаСервере
Процедура test_РасчетСкидки_Больше10000() Экспорт
    Скидка = МодульРасчетаСкидок.РассчитатьСкидку(15000);
    ЭтотОбъект.ПроверитьРавенство(1500, Скидка);
КонецПроцедуры
```

#### C.5 bsl-language-server

```json
{
  "$schema": "https://1c-syntax.github.io/bsl-language-server/schema.json",
  "language": "bsl",
  "diagnostics": {
    "compute": "on",
    "trigger": "onSave"
  }
}
```

#### C.6 Печатная форма

```bsl
&НаСервере
Функция ПечатьРеализации(Ссылка) Экспорт
    ТабДок = Новый ТабличныйДокумент;
    Макет = ПолучитьМакет("Реализация");
    ТабДок.ТолькоПросмотр = Истина;
    Возврат ТабДок;
КонецФункции
```

#### C.7 Загрузка файла

```bsl
&НаКлиенте
Процедура ЗагрузитьФайл(Команда)
    Описания = Новый ОписаниеПередаваемыхФайлов;
    Описание = Описания.Добавить();
    Описание.МаскаИмениФайла = "*.xlsx;*.xls";
    Описание.МаксимальныйРазмер = 10 * 1024 * 1024;
    Если Не НачатьПолучениеФайлов(Описания) Тогда
        Возврат;
    КонецЕсли;
КонецПроцедуры
```

#### C.8 Отправка письма

```bsl
&НаСервере
Процедура ОтправитьПисьмо(Получатель, Тема, Тело) Экспорт
    Профиль = Почта.СоздатьПрофильИзНастроек("ОсновнойSMTP");
    Письмо = Почта.СоздатьПисьмо(Профиль);
    Письмо.Тема = Тема;
    Письмо.Текст = Тело;
    Почта.ДобавитьПолучателя(Письмо, Получатель);
    Почта.Отправить(Письмо);
КонецПроцедуры
```

#### C.9 Срез последних по курсу валют

```bsl
"ВЫБРАТЬ
|   К.Валюта    КАК Валюта,
|   К.Курс      КАК Курс,
|   К.Кратность КАК Кратность
|ИЗ РегистрСведений.КурсыВалют.СрезПоследних(&Дата, ) КАК К
|ГДЕ К.Валюта = &Валюта"
```

#### C.10 Пакетная обработка

```bsl
Пакет = 100;
Счетчик = 0;
НачатьТранзакцию();
Попытка
    Пока Выборка.Следующий() Цикл
        Счетчик = Счетчик + 1;
        Если Счетчик % Пакет = 0 Тогда
            ЗафиксироватьТранзакцию();
            НачатьТранзакцию();
        КонецЕсли;
    КонецЦикла;
    ЗафиксироватьТранзакцию();
Исключение
    ОтменитьТранзакцию();
    ВызватьИсключение;
КонецПопытки;
```

#### C.11 Асинхронная операция

```bsl
&НаКлиенте
Асинхронная Процедура ЗагрузитьДанные(Команда)
    Результат = Ждать HTTPВыполнитьЗапросAsync("https://api.example.com/data");
    ОбработатьРезультат(Результат);
КонецПроцедуры
```

### D. Глоссарий и ссылки

#### D.1 Глоссарий

| Термин | Значение |
|--------|----------|
| ИТС | Информационно-Технический Справочник 1С |
| БСП | Библиотека Стандартных Подсистем |
| СКД | Система Компоновки Данных |
| EDT | Enterprise Development Tools |
| RLS | Ограничение на уровне записей |
| BDD | Behavior-Driven Development |
| XDTO | XML Data Transfer Objects в 1С |
| UUID/GUID | Глобальный уникальный идентификатор |
| Async/Wait | Асинхронные процедуры и ожидание |
| Promise | Объект отложенного результата |
| TTL | Время жизни кэша |
| OpenMetrics | Стандарт метрик для систем мониторинга |

#### D.2 Базовые ссылки ИТС

| Стандарт | URL | Тема |
|----------|-----|------|
| `#std437` | https://its.1c.ru/db/v8std/content/437/hdoc | Оформление запросов |
| `#std438` | https://its.1c.ru/db/v8std/content/438/hdoc | Пустой результат |
| `#std435` | https://its.1c.ru/db/v8std/content/435/hdoc | Полное внешнее соединение |
| `#std412` | https://its.1c.ru/db/v8std/content/412/hdoc | Сортировка |
| `#std762` | https://its.1c.ru/db/v8std/content/762/hdoc | Локализация |
| `#std441` | https://its.1c.ru/db/v8std/content/441/hdoc | Общие требования к коду |
| `#std452` | https://its.1c.ru/db/v8std/content/452/hdoc | Транзакции |
| `#std431` | https://its.1c.ru/db/v8std/content/431/hdoc | Общие модули |
| `#std573` | https://its.1c.ru/db/v8std/content/573/hdoc | Построение форм |
| `#std574` | https://its.1c.ru/db/v8std/content/574/hdoc | Клиент-сервер |
| `#std580` | https://its.1c.ru/db/v8std/content/580/hdoc | Динамические списки |
| `#std600` | https://its.1c.ru/db/v8std/content/600/hdoc | Производительность запросов |
| `#std500` | https://its.1c.ru/db/v8std/content/500/hdoc | Журнал регистрации |
| `#std660` | https://its.1c.ru/db/v8std/content/660/hdoc | Обработчики обновления |

### E. Статус переноса из архива

#### E.1 Уже перенесено в новую структуру

- универсальные правила по запросам;
- базовые правила BSL, типы данных, коллекции, исключения;
- формы, клиент/сервер, транзакции, производительность;
- базовые правила по XML, формам, `ChildObjects`, `UUID`, `InternalInfo`;
- учебный профиль `EDU`;
- промышленный профиль `PROD`;
- сложный профиль `ENT`;
- развилка по архитектуре отчетов;
- профилированный `Self-Check`;
- минимальные блоки по `CI/CD`, наблюдаемости и тестированию;
- reference snippets по `HTTP`, `COM`, `Vanessa Automation`, `xUnitFor1C`, `bsl-language-server`;
- прикладные примеры по печати, файлам, email и валютам.

#### E.2 Еще можно переносить выборочно

- большие готовые пайплайны `GitHub Actions` и `Jenkinsfile`;
- расширенные шаблоны наблюдаемости, `OpenMetrics`, `Grafana`, `Prometheus`;
- редкие и длинные XML-эталоны;
- крупные примеры сложной `СКД`;
- специальные примеры для нестандартных интеграций и редких инфраструктурных сценариев.

#### E.3 Правило дальнейшего переноса

- переносить из архива только то, что реально усиливает новый стандарт;
- не возвращать в основной текст большие дубли и длинные листинги без пользы;
- крупные примеры держать в приложениях, а не в универсальном ядре;
- если блок уже перенесен и покрыт в новой структуре, архив использовать только как историческую справку.

#### E.4 Уже стабилизированные разделы

- `0. Мандат документа`;
- `1. Протокол работы AI по 1С`;
- `PART A — Универсальное ядро`;
- `PART B — Учебные задачи`;
- `PART E — Выбор архитектуры отчета`;
- `PART F — XML и управляемые формы`;
- `PART G — Self-Check`.

#### E.5 Что еще можно усиливать без смены структуры

- прикладные примеры в `PART C` для типовых конфигураций и расширений;
- инфраструктурные примеры в `PART D` для `CI/CD`, мониторинга и интеграций;
- приложения `A-C`, если появятся локально подтвержденные эталоны, а не просто длинные листинги;
- глоссарий и ссылки, если появится устойчивый набор реально используемых источников проекта.
